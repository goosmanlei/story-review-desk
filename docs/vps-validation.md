# VPS 发布器本机验收

验证日期：2026-09-11。首轮软件工作基线：`ebfb520c7f0493dabf66cc8fc6b1c94be0725021`。下表严格区分实现、真实本机观察和未观察项；本记录不是生产部署证明。

## 根路径组合版本补充

经用户明确授权，将主分支内存修复 `28b446f` 合入 VPS 发布分支（合并提交 `7424cde`），增加 HTTPS IP 根路径支持。HTTP 仍不承载业务或认证，只允许独立配置的 ACME challenge 与固定 HTTPS 跳转。

组合版本全量发现测试：2,238 项，2,165 通过、73 按条件跳过、0 失败；Python 历史 AST 使用 3.11。VPS 专项 50 项、主分支读取投影定向 8 项、根路径隔离浏览器 6 项、类型检查、构建与 HTTP 表现层检查通过。另验证全量只读备份的一小时有界期限、普通维护期限不变及脱敏错误分类。真实主机的证书、包、业务版本、恢复回执和浏览器观察由发起项目的 VPS 部署记录另行维护；以下首轮模拟故障证据不冒充新主机观察。

## 已观察

Codex 启动链补充：VPS 生成的 storage owner 使用 `review-vps-*`，通过现有实例加载器的身份格式门禁；回归覆盖实际 owner 文件生成。已冻结本地基线可独立重打软件，只复制登记内容、保留原 manifest 字节、拒绝损坏和覆盖。最新全量 2,247 项，2,174 通过、73 跳过、0 失败；VPS 专项 53 项、类型检查和构建通过。未发真实模型请求。

公开演示补充（用户明确选择匿名完整业务编辑）：合并 `38d86ca` 正式／试验历史兼容修复，增加显式 `PUBLIC_DEMO` 策略、页面提示及固定提交的独立只读备份镜像。最终全量 2,246 项，2,173 通过、73 跳过、0 失败；VPS 专项 52 项、备份专项 17 通过／1 跳过、根路径隔离浏览器 6 项，类型检查、构建及 HTTP 表现层检查通过。匿名请求不冒充认证用户，错误 Origin、伪造身份、旧运行期及将独立备份软件用于写入均被拒绝。公网读回和真实完整包仍由部署项目记录，不以这些单元证据冒充已上线。

| 项目 | 本机证据 |
| --- | --- |
| 完整包 | 软件清单、NDJSON PostgreSQL、登记 PNG、两份 Linux amd64 镜像；逐文件 SHA／大小、数据库语义、媒体退役协议与四遍恢复校验通过 |
| 连续发布 | A `5e3e7029…` → B `8e6f01b6…` → C `f73086e2…`；每版制造 REMOTE_DIRT，下一版恢复各自本地基线；C 的 backup 为 B |
| 干净回滚 | C → B，新 runtimeEpoch，业务身份和冻结中文／逻辑 URL 字节保持，远端配置草稿未回灌 |
| 浏览器 | 独立 loopback TLS Nginx，经 Basic Auth；六个工作区深链与刷新、业务配置草稿写入、图片 Range、合成 WAV／MP4 SHA 与 Range、浏览器媒体解码及下载通过；同源资源逃逸为零 |
| 访问控制 | 页面／API／媒体／静态／SSE 未认证 401；错误 Origin 403；直接 socket 伪造代理身份 403；旧页面写入 409 并显示刷新拦截；其他 Nginx location 字节与响应保持 |
| Codex | 缺凭据页面显示未连接；真实宿主桥接进程使用 mock-response，在页面提交、队列、模拟回复及 SSE 完成；会话属于新运行期；真实模型调用为零 |
| 中断续传 | 与 SSH 相同的二进制控制器运行在本机子进程；在 SAVING 的 app.tar 第 65,536 字节断流，服务在线、前版 clean 保留、下一操作被拒绝；原命令续传及重复回执通过 |
| Nginx 失败 | 实际 Nginx 校验子进程失败后，修改前配置 SHA、挂载读回和 inode 一致；执行正常 reload |
| 保留和空间 | 两个 clean 槽、一份实例，scratch 完整归档数为零；逐阶段记录目录分配字节、PG 卷、镜像清单、宿主及 Docker 文件系统剩余空间 |

本机演练包明确标为 `UNVERSIONED`，不能通过远端发布入口。提交后的精确版本包另以其 manifest 与恢复回执核验；不能把开发包声明为精确发行版本。

## 空间样本

C 完成时：部署目录分配 **1,788,248,064 bytes**、PG 卷 **49,741,824 bytes**、运行镜像逻辑大小合计 **874,366,068 bytes**。新实例开放后回收旧 backup，保存前目录下降至 **898,469,888 bytes**。回滚不复制完整归档。镜像数字逐引用保守合计，不冒充 Docker 去重后的专属物理层占用；同时记录实际 Docker 文件系统容量，避免仅统计版本目录。

## 质量检查

- 完整单元套件：1,974 项，1,928 通过，46 按条件跳过，0 失败。历史 Python AST 验证使用明确 Python 3.11；未修改历史哈希。
- VPS／恢复／桥接专项：协议、目标防护、A/B/C 状态机、失败恢复、损坏／截断输入、空间不足、工作器未收尾、旧授权门禁、凭据缺失、模拟认证失败及不自动重试。
- `test:types`、Node 根路径构建、Linux amd64 VPS 构建、HTTP 表现层静态检查通过。
- 根路径隔离浏览器回归 6 项通过；Codex 自测通过。正式 3000、Sites、既有创作任务均未操作。

## 未验证／后续边界

- **BLOCKED（本轮明确禁止）**：真实 VPS、真实域名读写、升级后 Node／Compose／Nginx 实际状态、真实 Codex／提供方认证与模型调用。
- **未做真实故障注入**：公网 SSH 重连／主机重启、磁盘实际写满、真实外部执行结果未知。相应协议／状态机有本机故障测试，但不替代这些观察。
- 本机未逐一完整重走所有正式故事裁决 UI；本轮浏览器实际写入覆盖配置草稿，业务授权／CAS／恢复执行门禁由专项和既有测试覆盖。不能声称每类业务真实操作均已验收。
- 8 GiB VPS 满载、长期日志／业务增长和真实大库恢复时间未测。部署前重新预算与 inspect，不外推 synthetic 数据时延。
- 首轮开发期间远端 main 新增 `28b446f`，与存储模块重叠；当时采用独立交付分支。后续已获用户明确合并授权，组合版本复验见上节；未强推。

复现入口：`tests/vps-rehearsal-setup.mjs`、`vps-local-rehearsal.mjs`、`vps-codex-rehearsal.mjs`、`vps-interruption-rehearsal.mjs`、`vps-http-rehearsal.mjs`，均要求 `REVIEW_TEST_VPS=1` 和隔离 fixture 路径。原始 JSON／截图保存在发起项目的忽略输出目录，不放公开故事软件包。
