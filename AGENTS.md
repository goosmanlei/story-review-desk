# 通用软件工作规则

默认中文。先读 README、本文件、STATE，再按任务读必要专题。软件说明不构成故事事实；不预加载历史业务或未启用的协作工具。

- 本仓库只维护通用软件。业务项目消费精确 Git 提交生成的受管包；不在项目副本维护第二套源码。每个运行进程只读显式 REVIEW_INSTANCE_ROOT，不从父目录、旧导出或同名文件推断业务。
- PostgreSQL 的对象与不可变修订是业务权威。网页、review CLI 和助手共用 /api/v1 的类型、校验、业务服务和事务。接口不互调，页面及路由不承载业务规则。
- 修改携带 expectedVersion 和 operationId；多对象核对全部版本，同编号不同内容拒绝，历史只读追加。实际引用的精确版本决定失效范围，不依赖整剧发布号。
- 草稿、待审、采用、权利事实、实际输入锁定和生成授权含义独立。一次确认保存判断、采用头与失效结果；AI 建议先预览再应用草稿，不能自行采用或生成。原始资料只读；媒体绑定永久版本和 SHA，不能从文件存在推断实际观察。
- Web 不直接调用模型或制作程序。一个后台工作器执行有界任务，Codex 沿用宿主认证和版本化只读工具协议。结果未知先查原操作，不自动重复执行。测试使用受控模拟响应。
- 配置分别属于系统、项目、本机；密钥、机器路径、会话和执行资格只存本机。导入重置运行期并拒绝旧页面写入，不恢复队列或执行权限。
- 发布默认固定本机核心仓库当前分支 Git HEAD，从 Git 内容构建，排除未提交及未跟踪修改；部署工具不提交、拉取、合并或推送。运行与源码推送分别核验。
- 验收后正常提交并 push 公开核心仓库；业务项目在私库另提交。不得提交凭据、运行态或他人未确认改动，不强推。
- 构建、QA、发布与制作必须先登记任务和阶段；结束、失败、取消后精确清退。保留当前与上一受管恢复点，未知结果保留恢复输入；归属、引用或占用不明不删除，不全局 prune。封存备份完全排除于部署和清理之外。
- README 做导航，AGENTS 保存边界，STATE 只记录当前软件能力，专题单独维护稳定说明。项目规则与创作授权由其当前实例及本轮用户指令决定。
