# 软件状态

当前实现为 React / Next.js Node standalone、模块化 Node 服务、一个后台工作器与 PostgreSQL 对象存储。没有内置故事、媒体、数据库或创作授权。

- 正式任务：review-tasks Skill 与 tasks CLI v3 只在用户明确要求发布/add、执行/续办或管理时启用；普通对话、只读查询和任务管理软件讨论不入账。Main 同时协调最多 3 个不同正式任务，v3 `next` 只列出 WORKER_REQUIRED 候选，schedule 成功后才形成每项的根 WORKER；WORKER 通过受管工具自主派生、通信、读取、等待、验收和关闭任意深度后代，业务层无固定数量上限，实际并发仍由宿主容量、资源互斥和关闭能力限制。v3 节点执行权限与 Main 租约分离；Main 终端信号或租约到期会在未 stop/pause 时保存 detachRun 回执，原 WORKER、后代和受管命令按原范围继续，新 Main 核对回执后原子续管。项目专属 App Server 自动启动/健康复用，持久接收决定与执行树工具调用；WebSocket Unix 协议和实际并行证据分别核验。`tasks attach` 提供完整任务树和公开消息/工具事件观察，可聚焦节点 steer 活动轮次或续办 idle 节点；原消息回执丢失只按耐久 RPC 回执核对，不重发。决定须单独按原类型答复，`--read-only` 禁止写入，detach/Ctrl+C/Ctrl+D 不终止工作；原连接不可用时只读回退仅显示原持久收件箱中身份与回执匹配的已完成公开条目，不启动服务或冒充完整历史。Main 不在线时节点按原权限继续，并保留消息、结果和检查点待下次续管。旧 v1/v2 事件与节点保持只读兼容；默认收敛开放派工后升级，仅完整、无 issues 的 PAUSED 快照及其 checkpoint、workspace、原操作和 Agent 停止回执全部核验后可保留开放旧节点升级。升级不补发节点权限，旧节点仍只读 attach 并由 Main 收敛。指定任务完成后暂停补位、最近安全检查点暂停、跨会话恢复、固定七列审计和正式发布后的受管本地提交继续保留；CLI/受管进程/fake RPC/PTY 已提供隔离验证，实际项目继承与真实主会话协作仍须现场验收。

- 构建版本：部署启动时固定完整 Git 提交；受管源码 .review-managed.json、core-lock.json 与运行包 release.json 分别绑定同一提交。
- 运行绑定：显式项目 instance/instance.json；本机路径、凭据引用、服务回执位于 instance/runtime。
- 业务包：版本 2，模块分块 NDJSON、原始资料和 SHA 媒体；从正式 API 导入导出，流式处理，无整包 16 GiB 限制。
- 当前数据库 Schema：1。部署拒绝身份或不兼容 Schema，不自动接管其他数据库。
- 资源审阅目录：项目根 review-library 通过带扩展名的硬链接组织登记媒体及显式选择的文本，与原件共享文件内容；工作器自动维护，CLI 支持同步、全量校验和精确路径解析。导出为只读投影，格式版本 2，自动迁移旧软链目录；项目配置 reviewLibrary 随业务包往返。
- 界面复用重构前六入口的组件、布局与交互；工作链、集场卷宗、关系画板、素材卡、制作准备和系统维护共用 v1 服务。系统管理包含使用与初始化、系统配置、数据与运行、系统架构。编辑草稿、意见和阅读状态跨模块保留，历史来源按原修订读取。
- 全剧制作：镜头制作和场景剪辑共用左侧可折叠集场目录，分集成片为左侧集菜单；目录、正文分别滚动，子步骤保留集场、折叠与阅读位置，必要读取仅遮挡并禁用右侧内容，导航和草稿保护保持挂载。
- 助手讨论与修改预览分开；修改建议校验对象和来源版本，用户明确应用后只保存为草稿。评论优化、素材审阅和制作任务由单一后台工作器执行；测试使用受控模型响应。
- 实际输入、制作清单及确定性动态分镜保留精确依赖和独立审阅证明。登记、渲染、观察与采用分别记录，不用文件存在替代人工判断。
- 本地或 VPS 上线事实由目标实例的部署回执、实际 API 和工作器证明；源码存在不代表任何目标已上线。

- 系统架构手册：核心 docs/handbook 的 Markdown、声明式图源及 SVG，由构建工具编译后随 standalone 发布；系统管理提供只读阅读页签及通过现有接口读取的当前项目实例映射。
